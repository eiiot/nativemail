FROM node:22-alpine

WORKDIR /app

RUN npm install --omit=dev --ignore-scripts --no-save undici@8.10.2

COPY scripts/notification-relay.mjs ./scripts/notification-relay.mjs

ENV NODE_ENV=production
ENV PORT=8787

EXPOSE 8787

CMD ["node", "./scripts/notification-relay.mjs"]
